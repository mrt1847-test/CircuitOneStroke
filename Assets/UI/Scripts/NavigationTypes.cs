namespace CircuitOneStroke.UI
{
    /// <summary>Top-level screen mode: MainShell (tabs) or GamePlay (no bottom bar).</summary>
    public enum ScreenMode
    {
        MainShell,
        GamePlay
    }

    /// <summary>MainShell tab.</summary>
    public enum MainTab
    {
        Home,
        Shop,
        Settings
    }
}
